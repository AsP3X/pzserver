# Environment
.env
.env.*
!.env.example
!.env.production.example
.credentials.json
.firewall.conf
ACCESS.local.txt
*.local.txt

# PHP / Composer
/app/vendor/
*.cache

# Laravel
/app/storage/*.key
/app/storage/framework/cache/data/*
/app/storage/framework/sessions/*
/app/storage/framework/views/*
/app/bootstrap/cache/*
/app/public/hot
/app/public/storage
/app/public/docs

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Caddy (generated by setup.sh)
caddy/Caddyfile
caddy/certs/

# Database backups (local)
db-backups/

# Game server data (generated at runtime)
game-server/data/
backups/*.tar.gz

# Host-mapped PZ data (bind mounts — do not commit worlds/binaries)
/data/**
!/data/**/.gitkeep

# Do NOT add a .gitkeep to data/postgres, data/app-node-modules or data/app-build.
# Those dirs must look *empty* to the container on first boot:
#   - postgres: initdb refuses to initialize a PGDATA that contains any file,
#     including dot-prefixed ones, and the db container ends up in a restart loop.
#   - app-node-modules: entrypoint.sh seeds it only when `ls -A` reports it empty.
#   - app-build: the Vite resync wipes the dir, which would delete a tracked file.
# The Makefile's `ensure-data-dirs` target creates all of them via `mkdir -p`.

# Node (frontend - future)
node_modules/

# Second web stack (web/) build output
/web/api/target/
/web/ui/dist/

# Workshop build artifacts
workshop/KnoxRelay/Contents/mods/KnoxRelay/42/media/

# Test / Coverage
/app/.phpunit.result.cache
/app/coverage/
.playwright-mcp/
