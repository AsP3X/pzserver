services:
  # ---------------------------------------------------------------------------
  # PZ Game Server — Dedicated server via SteamCMD
  # Image and arch-specific config provided by docker-compose.{arm64,amd64}.yml
  # ---------------------------------------------------------------------------
  game-server:
    container_name: ${GAME_SERVER_CONTAINER_NAME:-pz-game-server}
    restart: unless-stopped
    # Allow clean world save on stop (PZ needs time to flush chunks)
    stop_grace_period: 60s
    # Architecture note: the game process and web-api both write the shared
    # Lua bridge under ./data/zomboid/Lua. Prefer world-writable files (0666) and
    # non-sticky dirs (0777). Optionally align UIDs with
    # user: "${PZ_SHARED_UID:-1000}:${PZ_SHARED_GID:-1000}" on game-server when
    # your host image supports a non-root game process (not all do).
    ports:
      - "${PZ_GAME_PORT:-16261}:${PZ_GAME_PORT:-16261}/udp"
      - "${PZ_DIRECT_PORT:-16262}:${PZ_DIRECT_PORT:-16262}/udp"
    volumes:
      # Host-mapped game data (saves, Server/*.ini, logs, Lua bridge, etc.)
      - ${PZ_DATA_HOST:-./data/zomboid}:/home/steam/Zomboid
    networks:
      # Public: Steam / player traffic path (and outbound SteamCMD)
      - proxy-network
      # Private: RCON from web-api only
      - pzserver-internal

  # Live panel is web-ui + web-api + web-db from docker-compose.web.yml.
  # The Laravel app/queue/db/redis services were removed with the ./app tree.

  # ---------------------------------------------------------------------------
  # Docker Socket Proxy — Restricted Docker API access for web-api
  # ---------------------------------------------------------------------------
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:v0.4.2
    container_name: pz-docker-proxy
    restart: unless-stopped
    environment:
      CONTAINERS: 1
      POST: 1
      LOG: 1
      # Wipe needs to delete game-owned save trees from inside the container.
      EXEC: 1
      # Tile jobs join pzserver-internal (RCON save) and proxy-network (pzdataspec).
      NETWORKS: 1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - pzserver-internal

  # ---------------------------------------------------------------------------
  # Caddy — optional host reverse proxy (enabled with WEB_PROXY_MODE=caddy)
  # Host ports are added by docker-compose.web-caddy.yml
  # ---------------------------------------------------------------------------
  caddy:
    profiles: ["caddy"]
    image: caddy:2.11.2-alpine
    container_name: pz-caddy
    restart: unless-stopped
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy/certs:/etc/caddy/certs:ro
      - ${PZ_CADDY_DATA_HOST:-./data/caddy-data}:/data
      - ${PZ_CADDY_CONFIG_HOST:-./data/caddy-config}:/config
    depends_on:
      - web-ui
    networks:
      # Public edge (shared reverse-proxy network)
      - proxy-network
      # Reach web-ui on the private network
      - pzserver-internal

# -----------------------------------------------------------------------------
# Networks
# -----------------------------------------------------------------------------
# proxy-network      — public / shared edge (join existing proxy stack; external)
# pzserver-internal  — private stack network (web-db, docker-socket-proxy, RCON)
networks:
  proxy-network:
    name: proxy-network
    external: true
  pzserver-internal:
    name: pzserver-internal
    driver: bridge
    internal: true

# -----------------------------------------------------------------------------
# Persistent data is bind-mounted under ./data/, with one exception:
# pz-map-tiles-sqlite (the live tiles.sqlite pack). See docker-compose.web.yml.
# -----------------------------------------------------------------------------
#   ./data/zomboid           game saves, Server/*.ini, Lua bridge
#   ./data/server            SteamCMD dedicated server install
#   ./data/backups           panel backups
#   ./data/map-tiles         render scratch (html tree, texture cache); the pack is the volume
#   ./data/web-postgres      Panel PostgreSQL (web-db)
#   ./data/caddy-data        Caddy certificates / storage
#   ./data/caddy-config      Caddy config state
